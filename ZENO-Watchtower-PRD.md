# 案例：ZENO Watchtower 的 PRD + 验收标准

> **这份文档演示什么。** 我在 ZENO（我独立开发上线的 AI 判断工作台）的 V1.5 功能
> Watchtower 上，完整走一遍我主张的 AI 时代产品交付物栈：D0 问题陈述 → D1 收缩版
> PRD → D2 可运行原型（规范性标注）→ D3 验收标准（核心交付物）→ D4 迭代机制。
> 运作原则一句话：**每个次品都是标准的 bug report** —— 缺陷不归因于人，归因于
> 标准，且标准必须因此产生一个 patch。
>
> 背景一句话：ZENO 把项目的"已确认判断"（truth）作为持久状态，任何写入必须经
> 用户确认（candidate-first）。Watchtower 要解决的是:truth 一旦确认就静止了,
> 但**外部世界会让它过期**。

---

## D0 · 问题陈述（半页，kickoff 前）

**用户与场景。** ZENO 的用户是独立开发者/solo founder，项目周期以月计。他们的
truth graph 里有一类节点的有效性依赖外部条件——"OpenAI 按 token 计价"（constraint）、
"竞品 X 没有做移动端"（hypothesis）、"因为政策 Y 所以放弃方案 Z"（rejection）。
外部条件变了，这些判断就悄悄失效，而依赖它们的下游计划继续在错误地基上推进。

**证据（需求真伪）。** 内测期 3 个真实项目复盘：每个项目的 truth graph 中
15%–25% 的活跃节点引用了可被外部证伪的条件；其中 2 个项目发生过"依据已过期的
constraint 继续决策"的实例，用户均在数周后才自己发现。用户没有任何机制知道
"我确认过的判断哪些已经不成立了"。

**要做的事（一句话）。** ZENO 周期性地重新调查图中"外部世界能推翻的部分"，在
发现反证时以候选（candidate）形式报告"这个假设可能被推翻了"——不需要用户开口，
不淹没用户，永不直接改写 truth。

**Non-goals（明确不做，防 scope drift——对 agent 参与的研发尤其重要）：**

1. 不做真实时监控。承诺的是"带预算的自适应巡检"，不是 realtime。
2. 不做任何 truth 直写。最大输出是携带反证的 alert candidate，由用户确认或驳回。
3. 不监控 `goal` / `principle` 类节点（除非用户强制）。"你的目标还成立吗"是
   议程设定，议程属于用户，不属于系统。
4. 不做游离 watch。"帮我盯着这个网址"必须先落到一个图节点上，watch 永远锚定
   节点——否则 Watchtower 退化为通用网页监控器，偏离产品定位。
5. 不做多用户告警路由（V1 单判断人假设不变）。

---

## D1 · PRD（收缩版）

> **为什么这么短。** 可运行原型（D2）已承载"长什么样、怎么交互"；本文档只保留
> 原型表达不了的东西：意图与边界、关键决策及理由、数据与状态规则、验收入口。

### 1. 意图与边界

- 做什么：见 D0。触发方式两种——Zeno 自动建议的 watch（基于节点的外部暴露度 ×
  爆炸半径）＋ 用户手动"盯住这个"。
- 不做什么：见 D0 non-goals。
- 为什么现在做：L2 Research（证据表、research run、检索意图持久化）已上线，
  Watchtower 需要的全部钩子已就位；且这是 ZENO 自治阶梯 L2→L3 的下一级。

### 2. 关键决策日志（decision log，每条可回答 why）

| # | 决策 | 理由（why） |
|---|---|---|
| K1 | watch 资格 = 外部暴露度 × 爆炸半径，与节点 kind 无关；kind 只作自动建议的先验 | "值得盯什么"是接地属性不是类型属性；依赖图本身就是优先级函数，不另造打分体系 |
| K2 | 巡检自动跑（最便宜档），升级到重推理必须用户手动开 | 调查是系统的本分（成本有界），重花费必须落在用户意图之内——自治不等于自作主张 |
| K3 | 巡检频率与告警频率解耦：巡检可以每天，告警是稀缺资源（去重＋冷却＋每周上限） | 用户的确认注意力是最稀缺资源；调查量不允许兑换成打扰量 |
| K4 | 告警确认率是 L3 健康度指标；确认率下降时收紧告警准入，**禁止**通过把告警写得更"顺耳"来提升确认率 | 指标必须不可 gaming：告警永远携带反证证据本身，让证据而非措辞说服用户 |
| K5 | 一次巡检 = 一条 `research_run` 记录，不新建活动流水基础设施 | 复用 L2 已有管线；少造轮子，少一套要维护的状态 |
| K6 | rejection 节点纳入自动建议（"翻案 watch"）：当年拒绝方案 X 的理由 Y 失效时告警 | 机会成本告警是差异化能力——别人监控"你信的还对吗"，我们还监控"你放弃的该捡回来吗" |
| K7 | 预算耗尽 → 全部 watch 显式 paused ＋ 页面横幅，绝不静默降级 | 用户对"有人在帮我盯着"的信任建立在可见性上；静默失效比没有此功能更糟 |

### 3. 数据与状态规则（原型演示不出的部分）

- **表结构**：`watch`（锚定 `ir_nodes.id` 非空；`origin` = zeno_suggested |
  user_requested；`cadence` 四档 + 自适应退避状态；`signal_response` =
  report_only | investigate | deep_dive；`status` = active | paused |
  exhausted | dismissed；预算与花费字段）。`research_run` 增加
  `run_type`（research | patrol | escalation）和可空 `watch_id`。
- **状态机**：active ↔ paused（用户开关或预算耗尽）；exhausted 仅由预算触发、
  恢复预算自动回 active；dismissed 为终态。信号不是独立表——信号 = 结果非空的
  patrol run，以内容哈希去重。
- **权限/写入边界**：Watchtower 全部产出的写入路径与 MCP 相同——candidate-first，
  服务端强约束，不存在任何直接改 `ir_nodes.status` 的代码路径。
- **计费口径**：巡检走 economy 档模型位（`watch_patrol`），升级调查走 standard，
  深究走 frontier；三档均不受前台质量旋钮影响。每项目每月自治预算独立核算。
- **并发**：单一 Cron 入口按优先级扫到期 watch，游标持久化、分批自链；同一
  source URL 被多个 watch 引用时每批只抓取一次。

### 4. 验收入口

验收标准见 D3（独立版本化文档，`watchtower-acceptance-v0.x`）。本 PRD 不复述
任何验收细节——PRD 说明意图，D3 定义"什么算做对了"。

---

## D2 · 可运行交互原型（说明页）

原型用扣子编程搭建（dogfooding），覆盖：Watchtower 管理面板（watch 列表行：
目标节点 / 来源徽标 / 监控原因 / 频率 / 推理档位 / 上次巡检 / 开关）、节点详情页
Monitoring 区、告警候选卡片（含反证证据 diff）、预算横幅。

**规范性标注（normative vs illustrative）——不标注的后果是 agent 把原型里的
偶然细节当成需求原样复制：**

| 部分 | 标注 | 说明 |
|---|---|---|
| 告警候选必须内联展示反证证据（原引文 vs 现状 diff） | **normative** | K4 的载体，不可省略 |
| 监控原因（reason）字段对 Zeno 建议的 watch 必填且用户可读 | **normative** | 议程透明的落点 |
| 推理三档的用户措辞：哨兵/追查/深究，不暴露模型名 | **normative** | 易学易用承诺 |
| 预算耗尽横幅的存在 | **normative** | K7 |
| 列表排序交互、徽标配色、卡片圆角、面板宽度 | illustrative | 研发/agent 可自行改进 |
| 巡检历史的展示形式（时间线 vs 表格） | illustrative | 待用户反馈收敛 |

---

## D3 · 验收标准（核心交付物）

> `watchtower-acceptance-v0.1` · 2026-07-20 · changelog 在文档尾部
> 每条 case 标注来源（哪个决策/缺陷产生了它）——provenance 是这套标准的
> 一等公民，和 ZENO 里每个 truth 节点带 rationale 是同一个思想。

### 3a. 机器可判层（test cases / eval set）

**确定性行为（e2e / 集成测试，硬验收）：**

| ID | 输入 → 期望输出 | 来源 |
|---|---|---|
| WT-01 | 带 L2 证据的 `hypothesis` 节点存在 → Watchtower 出现一条 zeno_suggested watch，reason 非空且含依赖链摘要 | K1, K2 |
| WT-02 | 巡检发现存储引文在源页面消失 → 产生 signal；`report_only` 下落一条 alert candidate，payload 含证据 diff | K4 |
| WT-03 | `signal_response = investigate` 时同一信号 → 触发一次 escalation run，花费 ≤ 单次上限 | K2 |
| WT-04 | 相同内容哈希的信号在冷却期内再次出现 → 不产生第二条告警，仅记 patrol log | K3 |
| WT-05 | 一周内第 N+1 条告警（N 为项目周上限）→ 被抑制并入队，不投递 | K3 |
| WT-06 | 项目月度预算耗尽 → 该项目全部 watch 状态置 `exhausted`，Watchtower 顶部出现横幅 | K7 |
| WT-07 | 用户自设 constraint（如"预算 5 万"，无外部接地）→ 永不出现在自动建议中 | K1 |
| WT-08 | `goal` / `principle` 节点 → 无自动建议；用户强制创建 watch 成功 | D0-NG3 |
| WT-09 | 两个 watch 引用同一 source URL → 单批巡检中该 URL 仅抓取一次 | K5, D1§3 |
| WT-10 | 连续 3 次无信号巡检 → 巡检间隔退一档（封顶 monthly）；出现信号 → 间隔收紧一档 | D1§3 |
| WT-11 | **数据库断言**：全量回归后，`ir_nodes` 中不存在任何一行的状态变更源于 watchtower 代码路径 | Iron Law, D0-NG2 |
| WT-12 | rejection 节点 rationale 引用外部事实 → 出现翻案类建议 watch，reason 指明"当年因 Y 拒绝，Y 可能已变化" | K6 |

**模型判定环节（golden set 分级样例）——"页面实质性变化"检测器：**

- 语料：30 组（源页面快照 A, 快照 B, 标签）三元组，标签 ∈ {signal, no_signal}。
- 必含分级样例：引文原文消失（signal）/ 页面改版但引文语义保留（no_signal，
  最容易误报的一类）/ 价格数字变更（signal）/ 仅广告与页脚变动（no_signal）/
  页面 404（signal，独立类别）。
- 通过线：signal 召回 ≥ 90%，误报率 ≤ 15%（v0.1 起点，见 3d 探索项）。
- 每次改动检测逻辑（prompt、哈希策略、模型）必须全量重跑该 golden set。

### 3b. 人判层（rubric ＋ 好/坏对照）

**R1 · 监控原因（reason）质量**——光写"可读"没有判定力，给对照：

- ✅ 合格：「盯住此假设：其证据引用了 OpenAI 定价页（4 月抓取），且 3 个活跃
  计划经 depends_on 依赖它。定价变动会连锁影响成本模型。」
  （判据：说出外部依赖是什么 + 爆炸半径多大 + 为什么值得盯）
- ❌ 不合格：「正在监控该节点。」（无信息量）
- ❌ 不合格：「该节点很重要，建议持续关注外部动态以保证信息新鲜度。」
  （全是形容词，无一可证伪）

**R2 · 告警候选质量**：

- ✅ 合格：「假设"竞品 X 无移动端"可能被推翻。反证：X 官网 7 月 12 日新增
  App Store 链接（截图＋引文对照）。建议动作：revisit，或 supersede 为
  "X 已于 2026-07 上线移动端"。」
- ❌ 不合格：「检测到相关页面有更新，请查看。」（把判断成本原样退还给用户）
- ❌ 不合格：任何不带反证证据、只有结论的告警（违反 K4，同时命中反例 N2）

### 3c. 反例集（不允许发生什么——信息密度高于正例）

| ID | 永不事件 | 来源 |
|---|---|---|
| N1 | Watchtower 任何路径直接改写 truth 状态 | Iron Law |
| N2 | 投递不携带反证证据的告警 | K4 |
| N3 | 任何形式的静默失效：预算耗尽不提示、watch 被丢弃不留痕、巡检长期失败无标记 | K7 |
| N4 | 每信号即告警（绕过去重/冷却/周上限的任何路径） | K3 |
| N5 | 为拉高确认率而软化告警措辞、隐藏不利证据（指标 gaming） | K4 |
| N6 | 自动建议 watch 出现在 goal/principle 或用户自设 constraint 上 | K1, D0-NG3 |
| N7 | 巡检花费突破 economy 档（如错误路由到 frontier 模型跑日常巡检） | D1§3 计费口径 |

### 3d. 严格度分级：探索项交付"假设 ＋ 指标"，不交付硬 spec

以下参数**未收敛**，硬性写死是浪费，也必然被现实打脸。各交付一条假设与判定指标：

| 探索项 | 当前假设（v0.1 起点值） | 判定指标 | 决议时点 |
|---|---|---|---|
| 变化检测阈值 | 召回 90% / 误报 15% 可用 | 由误报驱动的告警驳回占比 < 30% | 内测 4 周 |
| 自适应退避参数 | 3 次无信号退一档 | 退避后漏报（用户手动发现的过期节点）≈ 0 | 内测 4 周 |
| 告警周上限 N | N = 5 | 告警确认率 ≥ 50% 且用户无"被淹没"反馈 | 内测 4 周 |
| 翻案 watch 价值 | 用户会确认翻案告警 | 翻案类确认率 ≥ 普通类的 70% | V1.5 全周期 |

**北极星**：告警确认率（用户确认 / 投递总量）。它同时度量准确性和打扰度，
且因 K4 禁止措辞优化而不可 gaming。

### Changelog

- v0.1（2026-07-20）：首批 12 条机器判定 case + 2 组 rubric + 7 条反例。
  来源全部为设计决策（K1–K7）与 non-goals；尚无逃逸缺陷来源的 case——
  **这正常：缺陷来源的 case 只能靠 D4 闭环长出来，追求首日完备违背迭代论。**

---

## D4 · 迭代机制（把"标准的算法更新"落成流程)

**触发器（每个都生成一个标准 patch 候选）：**

- 逃逸缺陷：内测用户报告的每个 Watchtower 缺陷 → 归因到 D3 的缺口（缺 case？
  rubric 无判定力？反例集漏了一类？）→ 补一条带来源标注的 case。骂人不产生
  case，归因才产生。
- 研发假设：标准未覆盖时研发**不阻塞等产品**，按最合理假设先做并记入假设日志；
  假设进入下一版标准（要么转正为 case，要么被推翻并补反例）。
- 用户反馈与 3d 探索项到期：数据回来 → 探索项要么硬化为 3a 的 case，要么修正
  假设续期。

**节奏**：每周一次 spec diff review——像 code review 一样 review 验收标准的
变更：新增了哪些 case、来源是什么、哪个探索项该硬化了。标准文件进 git，diff
可见。

**假设日志示例（研发侧的对等交付物）：**

> 2026-07-18 · 标准未定义"源页面持续 404 超过 30 天"的 watch 处置。
> 假设：置 paused 并在 reason 追加"源已失效"，不自动 dismissed（用户可能想
> 换源续盯）。→ 待下版标准裁决：转正 or 推翻。

**闭环声明**：kickoff 首日交付的最小集 = D0 + D2 happy path + D3 的 12 条
case + non-goals。不追求完备，追求**闭环从第一天就在转**——标准从 v0.1 开始长，
每个次品都是它的 bug report。

---

## 附：面试讲法（30 秒版）

1. **同构点**：我给研发的 D3（定义"什么算好"并尽量机器可判化）和扣子编程要帮
   用户对他们的 agent 做的"自动验收"是同一件事——我的协作模式就是产品核心机制
   在组织层的映射。
2. **一手体感**：这不是纸面方法论。ZENO 的 candidate-first 写入、judge 评分、
   badcase 归因（证据不足 40%→18%）就是这套东西的单人实践版，Watchtower 的
   设计稿里验收标准和 open questions 是分开写的——收敛的硬验收，没收敛的留假设。
3. **防守点**：如果被问"标准怎么可能写全"——它写不全，这是本质属性；所以核心
   交付物不是标准本身，是 D4 那个让标准持续变准的闭环。
