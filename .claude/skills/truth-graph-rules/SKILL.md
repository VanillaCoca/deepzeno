---
name: truth-graph-rules
description: >-
  TODO(你来写): 这一行决定路由,是整个 skill 最重要的部分。
  要求包含两个要素: (1) 这个 skill 是什么/管什么 (2) 什么时候该触发它。
  提示: 想想哪些用户指令应该命中它 —— "改 truth graph 布局"、"调节点颜色"、
  "加一种边类型"、"改全貌层/单链层交互"…把这些场景词写进去。
  反例(太模糊,不会触发): "truth graph 的一些规则"。
---

# Truth Graph 设计规则路由

修改 ZENO truth graph 相关代码(渲染、布局、交互、配色、边/节点语义)之前,
必须先读取本项目的设计规则文档,再动手。

## 规则文档索引

| 文档 | 内容 | 何时读 |
|---|---|---|
| `docs/zeno-truth-graph-rules.md` | V1 基线:两层视图、同屏并置、颜色/形状语义、不画线决策 | 任何 truth graph 改动都先读 |
| `docs/zeno-truth-graph-rules-amendment-1.md` | 修订案 №1:语义分层、五横向带、位置承担结构 | TODO(你来写): 什么时候读?和 v1 冲突时谁优先?(答案在修订案开头,把它写成一条明确指令) |
| `docs/zeno-truth-graph-tokens.css` | 设计 token(颜色、尺寸变量) | 涉及样式/配色时读,禁止硬编码颜色值 |
| `docs/ZenoTruthGraph.reference.jsx` | 参考实现 | 需要对照组件结构时读 |

## 硬性约束(违反即返工)

TODO(你来写): 从规则文档里挑 3~5 条"最容易被 AI 违反"的铁律写在这里。
这是唯一直接内联进 skill 正文的规则 —— 因为它们值得每次触发都占上下文。
提示: 想想哪些决策一个不知情的 AI 最可能"好心办坏事"?
例如: 全貌层默认画不画关系线? 用户没确认的东西能不能进 truth?
(自己去 rules.md §0/§1.1/§7 里找,用你自己的话写,每条一行。)

## 工作流

1. 判断改动涉及哪些文档,按上表读取(不要全读,按需读)。
2. 动手前,如果改动与任何硬性约束冲突,停下来向用户确认,而不是绕过。
3. 改完后,对照 tokens.css 检查没有引入硬编码样式值。
